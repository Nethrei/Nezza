const $=s=>document.querySelector(s),$$=s=>document.querySelectorAll(s);$$('[data-scroll]').forEach(b=>b.addEventListener('click',()=>$('#'+b.dataset.scroll)?.scrollIntoView({behavior:'smooth',block:'start'})));const nav=$('.liquid-nav');$('#navToggle')?.addEventListener('click',()=>{const open=nav.classList.toggle('open');$('#navToggle').setAttribute('aria-expanded',open)});const cards=[...$$('.deck-card')];let current=0;function renderDeck(){cards.forEach((c,i)=>{const d=(i-current+cards.length)%cards.length;c.style.zIndex=cards.length-d;c.style.opacity=d>4?'0':'1';c.style.transform=`rotate(${d%2?-2:2}deg) translate(${d*3}px,${d*8}px)`});$('#albumIndex').textContent=String(current+1).padStart(2,'0')}function move(n){current=(current+n+cards.length)%cards.length;renderDeck()}$('.prev')?.addEventListener('click',()=>move(-1));$('.next')?.addEventListener('click',()=>move(1));renderDeck();let startX=0;$('.deck')?.addEventListener('touchstart',e=>startX=e.touches[0].clientX,{passive:true});$('.deck')?.addEventListener('touchend',e=>{const dx=e.changedTouches[0].clientX-startX;if(Math.abs(dx)>45)move(dx<0?1:-1)},{passive:true});const audio=$('#birthdayAudio'),musicBtn=$('#musicBtn');musicBtn?.addEventListener('click',async()=>{if(!audio)return;if(audio.paused){try{await audio.play();musicBtn.textContent='❚❚';musicBtn.title='Jeda musik';musicBtn.setAttribute('aria-label','Jeda musik')}catch(err){musicBtn.title='Tekan lagi untuk memutar musik'}}else{audio.pause();musicBtn.textContent='▶';musicBtn.title='Putar musik';musicBtn.setAttribute('aria-label','Putar musik')}});audio?.addEventListener('ended',()=>{musicBtn.textContent='▶';musicBtn.title='Putar musik';musicBtn.setAttribute('aria-label','Putar musik')});
